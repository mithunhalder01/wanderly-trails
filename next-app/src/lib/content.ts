import { blogPosts, destinations, featuredDestinations, featuredPackages, getBlogPostById, getPackageById, getDestinationById, getRelatedPackages, packages } from "@/data/staticData";

export const staticContent = {
  destinations,
  packages,
  blogPosts,
  featuredDestinations,
  featuredPackages,
  getDestinationById,
  getPackageById,
  getBlogPostById,
  getRelatedPackages,
};

