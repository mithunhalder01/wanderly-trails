# TODO - wanderly-trails

## Completed
- ✅ Handpicked Experiences (ToursSection) mobile horizontal smoothness + vertical scroll issue fixed in `src/components/ToursSection.tsx`.
- ✅ DestinationDetail header layout cleaned in `src/pages/DestinationDetail.tsx` (rating/location/pricing alignment) and verified with `next-app` production build.

## Remaining
- ⬜ Add more destinations to home/featured list + ensure it renders in UI without breaking layout.

